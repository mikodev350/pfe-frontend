// src/api/apiResource.js

export const saveResource = async (resourceData) => {
  // Simulation d'une requête API pour sauvegarder une ressource
  console.log("Resource data saved:", resourceData);
  return { success: true };
};

export const fetchResources = async () => {
  // Simulation d'une requête API pour obtenir des ressources
  return [];
};
